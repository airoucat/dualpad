# macOS Local Context

This file is reserved for machine-specific context on a macOS workstation and is intentionally separate from the shared `AGENTS.md`.

## Fill Before Using This Repo On macOS

- 本机 `xmake` / 编译器工具链路径
- 本机 Skyrim 或替代验证环境路径
- 本机外部 live artifact 路径
- 本机推荐命令与验证流程

## Current Status

- 这个仓库当前还没有补齐 macOS 本机环境说明。
- 不要把 `AGENTS.win.md` 里的 Windows 绝对路径复制到共享文档里。
- 如果未来在 macOS 上使用本仓库，请先补齐本文件，再按 `AGENTS.md` 的默认工作流继续。
